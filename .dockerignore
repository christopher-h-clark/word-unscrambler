node_modules
dist
.git
.github
.env
.env.local
__tests__
e2e
docs
coverage
# .npmrc: If host .npmrc contains auth tokens for private packages, use Docker secrets or
# buildkit features (--secret) to provide credentials instead of copying .npmrc into image
npm-debug.log
.DS_Store
# Exclude markdown and git files to keep image lean. Reference docs available at deployment time.
*.md
.gitignore
